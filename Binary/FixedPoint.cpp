
#include<iostream>
using namespace std;
int fix(int a[], int low, int high, int n, int key){
    int mid ;
    mid = low + (high - low)/2;
    if(n = 1){
        return a[0];
    }
    if(a[mid] == key && mid == key){
        return mid;
    } 
    else if(a[mid] < )
}