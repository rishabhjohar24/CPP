#include<bits/stdc++.h>
using namespace std;
int main(){
    vector <int> A;
    int n, var, fs = 0, B, sum, bs = 0;
    cin>>n;
    for(int i = 0; i < n; i++){
        cin>>var;
        A.push_back(var);
    }
    for(int i = 0; i < B; i++){
        fs = fs + A[i];
        bs = bs + A[n - 1 - i];
    }
    for(int i = 0; i < B; i++){
        if()
    }
}