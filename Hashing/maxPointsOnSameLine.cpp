#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    int att[n][2];
    vector<pair<int , int> > point;
    for(int i = 0; i < n; i++){
        for(int j = 0; j < 2; j++){
            cin>>arr[i][j];
        }
        v.push_back(make_pair(arr[i][0], arr[i][1]));
    }
}