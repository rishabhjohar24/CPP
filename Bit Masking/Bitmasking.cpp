#include <bits/stdc++.h>
using namespace std;
// Remove LSB
//  n >> 1;

// Add jth object to subset
//  A |= (1 << j)

// Remove jth object from subset
//  A &= ~(1 << j)

// Check wether jth object is in subset
// (A & (1 << j)) ? "YES" : "NO"

// Flip jth item of set
// A ^= (1 << j)

// To get least significant bit
// A &=(-A)

// Turn all bits on
// A = (1 << n) - 1;

// iterate for(int x = 0; x < (1 << n); x++)


int main()
{
  int n;
  cin >> n;
}